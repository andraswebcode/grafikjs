type MouseOrTouchEvent = MouseEvent | TouchEvent;

type ModeType = 'select' | 'pan' | 'draw';

type DrawingToolName = 'pen' | 'bezier' | 'shape';

export { MouseOrTouchEvent, ModeType, DrawingToolName };
